apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: tinycode-network-policy
  namespace: tinycode
spec:
  podSelector:
    matchLabels:
      app: tinycode
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - port: 4096
          protocol: TCP
