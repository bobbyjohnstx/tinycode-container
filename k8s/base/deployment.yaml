apiVersion: apps/v1
kind: Deployment
metadata:
  name: tinycode
  namespace: tinycode
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tinycode
  template:
    metadata:
      labels:
        app: tinycode
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 0
      containers:
      - name: tinycode
        image: quay.io/bjohns/tinycode-container:latest
        ports:
        - containerPort: 4096
          protocol: TCP
        env:
        - name: TINYCODE_PORT
          value: "4096"
        - name: TINYCODE_SERVER_PASSWORD
          valueFrom:
            secretKeyRef:
              name: tinycode-auth
              key: server-password
              optional: true
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        # /global/health is now unauthenticated (fixed in tinycode issue #2).
        livenessProbe:
          httpGet:
            path: /global/health
            port: 4096
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /global/health
            port: 4096
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop: [ALL]
          seccompProfile:
            type: RuntimeDefault
        volumeMounts:
        - name: tinycode-data
          mountPath: /home/tinycode/.local/share/tinycode
          subPath: data
        - name: tinycode-data
          mountPath: /home/tinycode/.config/tinycode
          subPath: config
      volumes:
      - name: tinycode-data
        persistentVolumeClaim:
          claimName: tinycode-data
