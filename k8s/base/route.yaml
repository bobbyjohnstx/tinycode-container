apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: tinycode
  namespace: tinycode
spec:
  to:
    kind: Service
    name: tinycode
    weight: 100
  port:
    targetPort: 4096
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
